name: Greetings

on:
  push:
    branches:
      - master

jobs:
  greeting:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/first-interaction@v1
      with:
        push-mesage: 'Greetings, thanks $GITHUB_ACTOR for the commit :)'  
